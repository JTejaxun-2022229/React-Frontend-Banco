/*import UserProfile from "react-user-profile";

export class PerfilAdmin {
  render() {
    const userDetails = response.data.account;
    const photo ="";
    const userDPI = userDetails.DPI;
    const userName = userDetails.name;
    const userPhone = userDetails.phone;
    const userEmail = userDetails.email;
    const userRole = "You'r a Administrator"

    return (
      <div style={{ margin: "0 auto", width: "100%" }}>
        <UserProfile
          photo={photo}
          userDPI={userDPI}
          userName={userName}
          userPhone={userPhone}
          userEmail={userEmail}
          userRole={userRole}
          initialLikesCount={121}
          initialFollowingCount={723}
          initialFollowersCount={4433}
          initialComments={comments}
        />
      </div>
    );
  }
}*/